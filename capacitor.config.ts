import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.famprepplanapp',
  appName: 'Tahanang Handa',
  webDir: 'dist'
};

export default config;
