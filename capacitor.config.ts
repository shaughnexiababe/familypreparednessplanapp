import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.famprepplanapp',
  appName: 'Ligtas CamNorte',
  webDir: 'dist'
};

export default config;
